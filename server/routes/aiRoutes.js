const express = require("express");
const router = express.Router();

// Placeholder route for AI-related endpoints
router.get("/", (req, res) => {
  res.send("AI route working!");
});

module.exports = router;
